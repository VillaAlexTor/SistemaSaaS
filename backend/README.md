# Backend

Aquí irá el proyecto Django más adelante.

Estructura prevista:
- `backend/` — código de Django (apps, settings, etc.)

Puedes añadir un archivo `.gitkeep` u otro placeholder si deseas mantener el directorio en el control de versiones.
